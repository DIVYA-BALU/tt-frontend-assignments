import { IsButtonDirective } from './is-button.directive';

describe('IsButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new IsButtonDirective();
    expect(directive).toBeTruthy();
  });
});
