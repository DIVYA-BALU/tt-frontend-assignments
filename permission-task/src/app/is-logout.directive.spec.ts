import { IsLogoutDirective } from './is-logout.directive';

describe('IsLogoutDirective', () => {
  it('should create an instance', () => {
    const directive = new IsLogoutDirective();
    expect(directive).toBeTruthy();
  });
});
