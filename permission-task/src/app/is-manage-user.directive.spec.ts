import { IsManageUserDirective } from './is-manage-user.directive';

describe('IsManageUserDirective', () => {
  it('should create an instance', () => {
    const directive = new IsManageUserDirective();
    expect(directive).toBeTruthy();
  });
});
