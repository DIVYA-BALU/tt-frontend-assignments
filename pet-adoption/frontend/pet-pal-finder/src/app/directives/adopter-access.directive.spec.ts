import { AdopterAccessDirective } from './adopter-access.directive';

describe('AdopterAccessDirective', () => {
  it('should create an instance', () => {
    const directive = new AdopterAccessDirective();
    expect(directive).toBeTruthy();
  });
});
