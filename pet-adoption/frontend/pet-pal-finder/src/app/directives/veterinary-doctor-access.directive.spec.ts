import { VeterinaryDoctorAccessDirective } from './veterinary-doctor-access.directive';

describe('VeterinaryDoctorAccessDirective', () => {
  it('should create an instance', () => {
    const directive = new VeterinaryDoctorAccessDirective();
    expect(directive).toBeTruthy();
  });
});
