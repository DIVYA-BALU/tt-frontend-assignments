import { ImagePathConverterPipe } from './image-path-converter.pipe';

describe('ImagePathConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagePathConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
