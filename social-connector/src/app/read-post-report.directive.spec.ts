import { ReadPostReportDirective } from './read-post-report.directive';

describe('ReadPostReportDirective', () => {
  it('should create an instance', () => {
    const directive = new ReadPostReportDirective();
    expect(directive).toBeTruthy();
  });
});
