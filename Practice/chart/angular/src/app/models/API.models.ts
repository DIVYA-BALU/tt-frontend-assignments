export interface UserEnrollmentDetails{
  month: string;
  year: string;
  count: number;
}

export interface CityCount{
  city: string;
  count: string;
}

export interface UserDensity {
  count: number;
  latitude: number;
  longitude: number;
}

