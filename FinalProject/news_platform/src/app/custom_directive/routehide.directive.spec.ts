import { RoutehideDirective } from './routehide.directive';

describe('RoutehideDirective', () => {
  it('should create an instance', () => {
    const directive = new RoutehideDirective();
    expect(directive).toBeTruthy();
  });
});
