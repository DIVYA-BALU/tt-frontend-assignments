import { BranchDirective } from './branch.directive';

describe('BranchDirective', () => {
  it('should create an instance', () => {
    const directive = new BranchDirective();
    expect(directive).toBeTruthy();
  });
});
